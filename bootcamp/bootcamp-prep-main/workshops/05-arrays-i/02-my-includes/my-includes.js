// YOUR CODE BELOW
function myIncludes(arr, searchVal) {
  // if (arr.includes(searchVal)) {
  //   return true;
  // } else {
  //   return false;
  // }

  if (arr.indexOf(searchVal) !== -1) {
    return true;
  } else {
    return false;
  }
}
